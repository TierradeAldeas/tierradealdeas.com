---
import { getIndex, getEntries } from "@lib/contentParser";
import { sortByDate } from "@lib/sortFunctions";
import type { HistoriaEntry } from "@/types";
import CollectionLayout from "@components/historia/CollectionLayout.astro";

const { slug } = Astro.params;
const currentPageIndex = slug && !isNaN(Number(slug)) ? Number(slug) : 1;

const entryIndex = (await getIndex("historia")) as HistoriaEntry;
const entries = (await getEntries("historia", sortByDate)) as HistoriaEntry[];

const entriesPerPage = 6;
const indexOfLastEntry = currentPageIndex * entriesPerPage;
const indexOfFirstEntry = indexOfLastEntry - entriesPerPage;
const currentEntries = entries.slice(indexOfFirstEntry, indexOfLastEntry);

const pageCount = Math.ceil(entries.length / entriesPerPage);

export async function getStaticPaths() {
  const entries = (await getEntries("historia", sortByDate)) as HistoriaEntry[];
  const pageCount = Math.ceil(entries.length / 6);
  const paths = [];
  for (let i = 1; i < pageCount; i++) {
    paths.push({ params: { slug: (i + 1).toString() } });
  }
  return paths;
}
---

<CollectionLayout
  entryIndex={entryIndex}
  entries={currentEntries}
  pageIndex={currentPageIndex}
  pageCount={pageCount}
/>
